import ProjectList from '../components/ProjectList'; // Import Home component

const ProjectPage = () => {
  return <ProjectList />; // This is now valid JSX, since Home is a valid component
};

export default ProjectPage;